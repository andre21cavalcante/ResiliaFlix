function controllerCEP(cep){
    // Manda para o Model fazer a validação e dá continuidade a pipeline
    modelCEP(cep);    
}

function controllerSenha(confSenha){
    // Manda para o Model fazer a validação e dá continuidade a pipeline
    modelSenha(confSenha);
}

function controllerEmail(email){
    // Manda para o Model fazer a validação e dá continuidade a pipeline
    modelEmail(email);
}

function controllerRG(e, rg){
    // Manda para o Model fazer a validação e dá continuidade a pipeline
    modelRG(e, rg);
}

function controllerBotao(e){
    modelBotao(e)
}